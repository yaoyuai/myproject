<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.course.mapper.CououtInfoMapper">

    <insert id="addCouout" parameterType="CououtInfoQo">
        insert into couout_info values(null,#{studentId},#{courseId},null,now(),null)
    </insert>
</mapper>