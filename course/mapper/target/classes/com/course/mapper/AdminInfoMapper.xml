<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.course.mapper.AdminInfoMapper">


    <select id="judgeLogin" resultType="AdminInfo" parameterType="AdminInfoQo">
        select * from admin_info where admin_name=#{adminName} and admin_pwd=#{adminPwd}
    </select>
</mapper>